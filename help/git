# 下载安装git
# sudo apt-get install git
# 查看git版本
# git --version

# 设置git用户名,邮箱
# git config --global user.name "D-Lansen"
# git config --global user.email "lc13347710373@gmail.com"
# 查看git用户名,邮箱
# git config --global user.name
# git config --global user.email

# 生成rsa密钥
# ssh-keygen -t rsa -C "lc13347710373@gmail.com"
# 复制/home/lichen/.ssh/id_rsa.pub内容到Github->Settings->SSH and GPG keys
# 查看ssh是否配置成功
# ssh -T git@github.com

# 克隆远程仓库
# git clone git@github.com:D-Lansen/openvpn.git
